# Archive — superseded artifacts

Early/superseded working material, moved out of the main `talk/` directory so that everything
there is current and accurate (per the 2026-06 cleanup). Nothing here is deleted — pull anything
back if needed.

- `decks/` — superseded deck versions: `v2–v4` (early/redo), the human-edited `v5` base, and
  `v6–v10` (intermediate build steps). The only current deck is `../berlin_deck_v11.pptx`.
- `analysis/` — superseded build scripts: `build_deck.py` (v2→v3) and `build_v5.py` (v4→v5).
  The current chain `build_v6.py … build_v11.py` + `add_speaker_notes.py` stays in `../analysis/`.
- `figures/` — early/superseded plots (incl. the **retracted** `conformity_vs_correctness.png`
  v18xx axon-GT work, and `rf_importance.png` superseded by `../fig_rf_importance_new.png`).
- `notes/` — early analysis notes (now consolidated into `../methodology_provenance.md`,
  `../transparency_failure_modes.md`, `../figure_descriptions.md`, `../nature_comms_draft.md`)
  plus the deck change logs `deck_v5_changes.md` and `deck_coherence_review.md`.
